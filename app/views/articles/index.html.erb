<%= render 'header' %>
<ul>
  <% @articles.each do |article | %>
    <li>
      <%= link_to(article.title, article_path(article)) %> - <%= link_to 'Remove', article_path(article), method: :delete, data: { confirm: 'Are you sure?' } %>
      <p><%= article.content %></p>
    </li>
  <% end %>
</ul>
